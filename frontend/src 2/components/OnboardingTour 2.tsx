import React from 'react';
// Lightweight skeleton for react-joyride integration
// Actual steps should be provided by the onboarding UX spec

const OnboardingTour: React.FC = () => {
  return (
    <div data-testid="onboarding-tour">
      {/* Replace with <Joyride /> integration when react-joyride is installed and steps are defined */}
      <p>Onboarding tour placeholder (joyride integration point).</p>
    </div>
  );
};

export default OnboardingTour;
